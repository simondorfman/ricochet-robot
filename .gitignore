# never commit real secrets
config.php
rr-config.php
.env
# Per-server Apache overrides (secrets)
.htaccess.local

# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
